<svg is:raw style="width:100%;font-family:'Inter',serif;" viewBox="0 0 640 380" xmlns="http://www.w3.org/2000/svg">
          <text fill="var(--muted)" font-size="10" font-style="italic" opacity="0.7" text-anchor="middle" x="320" y="16">Where CAX sits relative to the existing framework landscape</text>

          <!-- Axes -->
          <!-- X: Individual encounter ←→ Longitudinal relationship -->
          <line opacity="0.15" stroke="var(--ink)" stroke-width="0.6" x1="40" x2="600" y1="200" y2="200"></line>
          <text fill="var(--muted)" font-size="8" opacity="0.5" x="40" y="215">Episodic</text>
          <text fill="var(--muted)" font-size="8" opacity="0.5" text-anchor="end" x="560" y="215">Longitudinal</text>
          <text fill="var(--muted)" font-size="8" font-style="italic" opacity="0.4" text-anchor="middle" x="320" y="358">← Time horizon of design →</text>

          <!-- Y: Output focus ↑↓ Transformation focus -->
          <line opacity="0.15" stroke="var(--ink)" stroke-width="0.6" x1="320" x2="320" y1="30" y2="360"></line>
          <text fill="var(--muted)" font-size="8" opacity="0.5" x="325" y="40">Transformation focus</text>
          <text fill="var(--muted)" font-size="8" opacity="0.5" x="325" y="350">Output focus</text>

          <!-- Quadrant labels faint -->
          <text fill="var(--muted)" font-size="8" font-style="italic" opacity="0.1" text-anchor="middle" x="170" y="125">Deep but short</text>
          <text fill="var(--muted)" font-size="8" font-style="italic" opacity="0.1" text-anchor="middle" x="480" y="125">Deep and sustained</text>
          <text fill="var(--muted)" font-size="8" font-style="italic" opacity="0.1" text-anchor="middle" x="170" y="285">Shallow and episodic</text>
          <text fill="var(--muted)" font-size="8" font-style="italic" opacity="0.1" text-anchor="middle" x="480" y="285">Shallow but sustained</text>

          <!-- FRAMEWORKS as dots -->

          <!-- ZMOT - episodic, output -->
          <circle cx="110" cy="290" fill="var(--muted)" opacity="0.45" r="5"></circle>
          <text fill="var(--muted)" font-size="8.5" x="120" y="288">ZMOT</text>
          <text fill="var(--muted)" font-size="7" opacity="0.5" x="120" y="300">Single decision moment</text>

          <!-- Consumer Decision Journey (McKinsey 2009) -->
          <circle cx="220" cy="255" fill="var(--muted)" opacity="0.45" r="5"></circle>
          <text fill="var(--muted)" font-size="8.5" x="230" y="253">McKinsey CDJ</text>
          <text fill="var(--muted)" font-size="7" opacity="0.5" x="230" y="265">Loop, not funnel</text>

          <!-- HubSpot Flywheel -->
          <circle cx="430" cy="270" fill="var(--muted)" opacity="0.45" r="5"></circle>
          <text fill="var(--muted)" font-size="8.5" x="440" y="268">Content Flywheel</text>
          <text fill="var(--muted)" font-size="7" opacity="0.5" x="440" y="280">Retention without depth</text>

          <!-- JTBD -->
          <circle cx="130" cy="160" fill="var(--muted)" opacity="0.55" r="5"></circle>
          <text fill="var(--muted)" font-size="8.5" x="140" y="158">JTBD</text>
          <text fill="var(--muted)" font-size="7" opacity="0.5" x="140" y="170">Job, not journey</text>

          <!-- Brand Purpose / Start with Why -->
          <circle cx="200" cy="110" fill="var(--muted)" opacity="0.55" r="5"></circle>
          <text fill="var(--muted)" font-size="8.5" x="210" y="108">Brand Purpose</text>
          <text fill="var(--muted)" font-size="7" opacity="0.5" x="210" y="120">Mission, not arc</text>

          <!-- Aggregation Theory (Ben Thompson) -->
          <circle cx="490" cy="235" fill="var(--muted)" opacity="0.55" r="5"></circle>
          <text fill="var(--muted)" font-size="8.5" x="500" y="233">Aggregation Theory</text>
          <text fill="var(--muted)" font-size="7" opacity="0.5" x="500" y="245">Distribution, not lens</text>

          <!-- Attention Economy (Tim Wu) -->
          <circle cx="160" cy="230" fill="var(--muted)" opacity="0.45" r="5"></circle>
          <text fill="var(--muted)" font-size="8.5" x="170" y="228">Attention Economy</text>
          <text fill="var(--muted)" font-size="7" opacity="0.5" x="170" y="240">Capture, not constitute</text>

          <!-- Product-Led Growth -->
          <circle cx="420" cy="310" fill="var(--muted)" opacity="0.45" r="5"></circle>
          <text fill="var(--muted)" font-size="8.5" x="430" y="308">Product-Led Growth</text>
          <text fill="var(--muted)" font-size="7" opacity="0.5" x="430" y="320">Usage ≠ understanding</text>

          <!-- CAX - starred, top right -->
          <circle cx="510" cy="85" fill="var(--accent)" opacity="0.8" r="8"></circle>
          <text fill="var(--accent)" font-size="10" font-weight="bold" x="524" y="83">CAX</text>
          <text fill="var(--accent)" font-size="7.5" opacity="0.7" x="524" y="95">Transformation + Time</text>

          <!-- Arrow from landscape toward CAX -->
          <path d="M350,185 Q430,140 500,92" fill="none" opacity="0.25" stroke="var(--accent)" stroke-dasharray="3,3" stroke-width="0.7"></path>

          <text fill="var(--muted)" font-size="8.5" font-style="italic" text-anchor="middle" x="320" y="373">This comparison places CAX around sustained engagement and changes in understanding.</text>
        </svg>