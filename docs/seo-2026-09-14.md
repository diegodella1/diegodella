# SEO, GEO and AEO implementation — 2026-09-14

## Changes

- One shared, deferred analytics entrypoint across all 42 HTML pages. It loads the existing GA4 property asynchronously only on the canonical HTTPS origin. Preview traffic is excluded.
- `generate_lead` for server-confirmed conversations; `updates_request_success` for confirmed update requests. Submission mode is captured when sending. Errors, malformed success bodies, timeouts and obsolete responses do not emit success events. Analytics failures cannot change delivery state. Custom parameters exclude contact fields and strip URL query/fragment data.
- Removed 27 per-page GA4 integrations. Versioned shared CSS/JS and CSS imports to avoid reusing old cached assets.
- Search/retrieval remains allowed; training robots are blocked. Google-Extended also controls some Gemini grounding, so its block covers both uses. Authentic Cloudflare crawler access remains an external verification.
- Converted 37 visual section labels to H2 across eight essays, preserving labels and anchors.
- Updated Work, Speaking and AI Media titles/descriptions and matching structured data. Added concrete starting points, links to relevant case studies, and the Knight Center's existing podcast-panel listing as independent background evidence.
- Revised unsupported blanket claims in Writing for the Filter. Clearly identified the two-filter framing as an interpretation and linked primary Google documentation where measurement and search behavior are discussed.
- Unified Person mainEntityOfPage references on About. Preserved URLs, canonicals, publication dates and the existing discovery formats.
- Discovery smoke distinguishes failed site assertions (exit 1), unresolved Cloudflare perimeter responses with Ray IDs (exit 2), and successful synthetic checks (exit 0). Synthetic success never substitutes for verified crawler logs.

## Validation

- Astro check: zero errors, warnings and hints.
- Site validation: 42 pages; shell, semantics, routes and local assets pass.
- Regression expectations retain the original migration baseline except four explicitly updated content/metadata snapshots. 44 parity checks passed.
- Browser contact scenarios pass with mocked email delivery: validation, failed delivery, success, malformed/empty JSON, offline, timeout, duplicate submission, close/reopen and update mode. Success tracking is checked separately from attempted submissions; throwing analytics does not stop success.
- Backend: 14 tests passed. Editorial documentation validation passed.
- Visual audit: 11 affected pages × mobile 320px and desktop, using downloaded production fonts; passed. Screenshots in `.editorial/seo-2026-09-14/`.

## External work not completed by repository changes

No authenticated Cloudflare, GA4, Search Console or Bing connector was available. No relevant account credentials were present in this process environment.

- Cloudflare: inspect real crawler identity and matched rules before changing the six synthetic 403 cases; retain training blocks and general WAF protection.
- GA4: mark `generate_lead` as a key event, keep update requests separate, review automatic form-interaction measurement and verify actual event receipt. No live email was sent by automated tests.
- Search Console/Bing: obtain the exports already discussed, verify canonical/indexing and sitemap status, check generative-search settings/reports where available, and inspect real Core Web Vitals.
- Revisit organic clicks, confirmed inquiries and AI referrals after 28 and 56 days. No rankings, citation gains or field performance claims are inferred from source checks.

Operational account steps: `docs/search-discovery-setup.md`. Crawler rationale and evidence: `docs/crawler-policy.md`.

## Release result

Activated `workspace-20260914T222821Z-16104`. All 134 release files matched the tested `dist` bytes. Previous active release: `workspace-20260914T192802Z-23522`; rollback with `rtk proxy bash scripts/deploy_release.sh --rollback workspace-20260914T192802Z-23522`.

The prepared release passed all 11 content/analytics/policy tests and 14 backend tests. Origin discovery passed 324 assertions. Public discovery passed 312 site assertions and returned exit 2 for six unresolved synthetic Cloudflare blocks:

| Simulated agent | Status | Ray ID |
| --- | --- | --- |
| ChatGPT-User | 403 | a3b2d90dde720ec2-EZE |
| OAI-SearchBot | 403 | a3b2d90dec000ec1-EZE |
| Claude-SearchBot | 403 | a3b2d90dfe8e0ec2-EZE |
| Claude-User | 403 | a3b2d90e0c110ec1-EZE |
| PerplexityBot | 403 | a3b2d90e1ea80ec2-EZE |
| Perplexity-User | 403 | a3b2d90e2c240ec1-EZE |

A separate before/after mobile performance experiment could not complete: Chromium startup/navigation timed out, including retries outside the sandbox. No comparative LCP/CLS/INP result is claimed. The successful visual and browser behavior audits are separate from performance measurement. Repeat performance measurement in a stable environment and obtain field CWV from Search Console.

Post-publication byte checks confirmed public Work HTML, analytics.js, versioned global.js and theme-contract.css match the release. The unversioned public `/robots.txt` still serves the prior origin body from Cloudflare cache (`cf-cache-status: HIT`, `cache-control: max-age=14400`, observed age 1769 at 22:38 UTC). The deployed origin has the new policy. Purge exactly `/robots.txt` in Cloudflare, or verify again after cache expiry; do not mark public policy alignment complete before checking the unversioned URL. This cache action also requires authenticated Cloudflare access.
