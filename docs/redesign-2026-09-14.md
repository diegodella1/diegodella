# Rediseño publicado — 14 de septiembre de 2026

Release activa: `workspace-20260914T235331Z-16099`.
Anterior: `workspace-20260914T222821Z-16104`.

## Resultado

Sistema visual oscuro con naranja y lima, Syne para títulos, Space Grotesk para lectura y JetBrains Mono para etiquetas. Fuentes WOFF2 locales con licencias OFL; sin Tailwind CDN ni dependencia de Google Fonts en las páginas publicadas.

Homepage organizada por tesis, escritos, productos reales, trayectoria y contacto. Navegación flotante compartida, menú móvil, tarjetas y estados coherentes. Artículos conservan una columna de lectura calma. Los textos principales de las 41 páginas interiores y sus canonical se verifican contra fixtures anteriores al rediseño; permanecen los IDs históricos y las rutas existentes.

No se incorporaron métricas, garantías de respuesta ni estados operativos ficticios del mockup. Formularios conservan sus contratos y enlaces de respaldo.

## Validación

- Astro: 137 archivos, sin errores, advertencias ni hints.
- Validación estática: 42 páginas, semántica, shell, rutas y recursos locales.
- Tests Node: 13 aprobados; migración: 44 aprobados; backend: 14 aprobados.
- Pruebas de navegador: navegación, Nuggets, filtros, teclado, clipboard, reduced motion, formularios y sus estados de fallo. Sin enviar correos reales.
- Auditoría de contraste y geometría: 42 páginas × 320px/escritorio, incluyendo estados interactivos. About además a zoom 200%; homepage repetida después del ajuste final del footer.
- Revisión visual independiente: disposición final **ship**. Resueltos recorte de navegación móvil, apellido del footer y palabra partida en contacto.
- Comparación byte a byte: los 145 archivos de la release coinciden con el build final.
- Servidor de producción: 324 comprobaciones de discovery aprobadas.
- HTML de homepage y CSS de diseño públicos coinciden byte a byte con el build final.

Capturas: `.editorial/neon-final/`. Sistema y mantenimiento: `DESIGN.md`, `.impeccable/design.json` y `docs/styles.md`.

## Límites y pendientes

El smoke público aprobó 312 comprobaciones de sitio, pero devuelve exit 2 por seis respuestas 403 de Cloudflare a agentes de IA simulados. No demuestra bloqueo de crawlers auténticos: revisar identidad verificada y regla aplicada en eventos de Cloudflare. Ray IDs de esta ejecución: ChatGPT-User `a3b354a5998f0ec7-EZE`; OAI-SearchBot `a3b354a5a9940ec7-EZE`; Claude-SearchBot `a3b354a5b9a20ec7-EZE`; Claude-User `a3b354a5c9a90ec7-EZE`; PerplexityBot `a3b354a5d9b50ec7-EZE`; Perplexity-User `a3b354a5d9bd0ec7-EZE`. Configuración de key events GA4 no verificada en cuenta.

El diagnóstico local de rendimiento usa archivos, 390px, CPU 4× y una ejecución por página. No es un benchmark público ni permite afirmar mejoras en Core Web Vitals. No detectó overflow en las cuatro páginas muestreadas. `writing-for-the-filter.html` mostró CLS 0,116 (antes 0,105); queda pendiente medir estabilidad con HTTP y fuentes en condiciones de producción. Datos completos: `.editorial/neon-final/lab-performance.json`.

El repositorio conserva cambios anteriores no commiteados; la publicación usa un snapshot del workspace. La documentación permanece en el repositorio, fuera de la distribución pública.

## Rollback

```sh
rtk proxy bash scripts/deploy_release.sh --rollback workspace-20260914T222821Z-16104
```

El script verifica el manifiesto y cambia el enlace activo de forma atómica.
